## dynamoAndS3 consists of java source (.java) files of a program that read/write to a DynamoDB and an S3 bucket.
-------------------------------------------------------------------------------------------------------------------
## apacheLambdaAndStorage consists of html files for a website (used Apache2 hosted on an EC2 instance). And python files (ran using Lambda) that gets data from the DynamoDB for the website.
